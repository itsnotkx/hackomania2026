---
phase: 03-android-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - NoScamAndroid/package.json
  - NoScamAndroid/android/app/src/main/AndroidManifest.xml
  - NoScamAndroid/android/build.gradle
  - NoScamAndroid/android/app/build.gradle
  - NoScamAndroid/tsconfig.json
autonomous: true
requirements:
  - ANDR-01

must_haves:
  truths:
    - "React Native 0.74 bare project exists and builds (npx react-native run-android succeeds on a connected device)"
    - "AndroidManifest.xml declares all required permissions: RECORD_AUDIO, SYSTEM_ALERT_WINDOW, FOREGROUND_SERVICE, FOREGROUND_SERVICE_MICROPHONE, REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"
    - "AudioCaptureService is declared in AndroidManifest.xml with android:foregroundServiceType=microphone"
    - "react-native-audio-record and react-native-float-bubble are installed and linked"
    - "@supersami/rn-foreground-service is installed and linked"
  artifacts:
    - path: "NoScamAndroid/android/app/src/main/AndroidManifest.xml"
      provides: "All Android permission declarations and service registration"
      contains: "foregroundServiceType=\"microphone\""
    - path: "NoScamAndroid/package.json"
      provides: "Project dependencies"
      contains: "react-native-audio-record"
    - path: "NoScamAndroid/android/app/build.gradle"
      provides: "Android build config with correct compileSdkVersion (34+)"
  key_links:
    - from: "NoScamAndroid/android/app/src/main/AndroidManifest.xml"
      to: "AudioCaptureService"
      via: "service element with foregroundServiceType"
      pattern: "foregroundServiceType=\"microphone\""
    - from: "NoScamAndroid/package.json"
      to: "react-native-audio-record"
      via: "npm dependency"
      pattern: "react-native-audio-record"
---

<objective>
Bootstrap the React Native Android project with all native dependencies installed and the complete AndroidManifest.xml configured for microphone foreground service, overlay, and battery optimization permissions.

Purpose: Every subsequent plan depends on this foundation. The AndroidManifest.xml is the hardest part to get right on Android 14 — declaring foregroundServiceType="microphone" and SYSTEM_ALERT_WINDOW correctly here prevents silent runtime failures later.

Output: A buildable bare React Native 0.74 TypeScript project in NoScamAndroid/ with all three native modules installed and a fully-configured AndroidManifest.xml.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-android-client/3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold bare React Native 0.74 TypeScript project</name>
  <files>
    NoScamAndroid/package.json
    NoScamAndroid/tsconfig.json
    NoScamAndroid/index.js
    NoScamAndroid/App.tsx
  </files>
  <action>
    Run from C:/Code/noscam/:

    ```
    npx react-native@0.74 init NoScamAndroid --template react-native-template-typescript
    cd NoScamAndroid
    ```

    Then install the three required native modules:

    ```
    npm install react-native-audio-record react-native-float-bubble @supersami/rn-foreground-service
    ```

    After install, run the Android native build to link the modules:

    ```
    cd android && ./gradlew clean && cd ..
    ```

    If `./gradlew clean` fails due to JDK version, ensure JAVA_HOME points to JDK 17 (React Native 0.74 requires JDK 17). Do NOT use Expo — this must be a bare project to access raw PCM bytes from react-native-audio-record.

    Replace the default App.tsx content with a minimal placeholder that will be replaced in later plans:

    ```tsx
    import React from 'react';
    import { View, Text, StyleSheet } from 'react-native';

    export default function App() {
      return (
        <View style={styles.container}>
          <Text style={styles.text}>NoScam — initializing...</Text>
        </View>
      );
    }

    const styles = StyleSheet.create({
      container: { flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#111' },
      text: { color: '#fff', fontSize: 16 },
    });
    ```
  </action>
  <verify>
    Run `ls NoScamAndroid/node_modules/react-native-audio-record` — directory must exist.
    Run `ls NoScamAndroid/node_modules/react-native-float-bubble` — directory must exist.
    Run `ls NoScamAndroid/node_modules/@supersami/rn-foreground-service` — directory must exist.
    Run `ls NoScamAndroid/android/gradlew` — Gradle wrapper must exist.
  </verify>
  <done>
    NoScamAndroid/ exists with all three native modules in node_modules. Gradle wrapper present. App.tsx contains placeholder component.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure AndroidManifest.xml with all required permissions and service declaration</name>
  <files>
    NoScamAndroid/android/app/src/main/AndroidManifest.xml
    NoScamAndroid/android/app/build.gradle
  </files>
  <action>
    Edit `NoScamAndroid/android/app/src/main/AndroidManifest.xml` to add all required permissions and the foreground service declaration. The final manifest must contain:

    ```xml
    <manifest xmlns:android="http://schemas.android.com/apk/res/android">

        <!-- Microphone capture -->
        <uses-permission android:name="android.permission.RECORD_AUDIO" />

        <!-- Floating overlay (SYSTEM_ALERT_WINDOW) -->
        <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

        <!-- Foreground service base permission -->
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

        <!-- Android 14+ REQUIRED: specific foreground service type for microphone access -->
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

        <!-- Battery optimization exemption (OEM devices kill background services) -->
        <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

        <application
            android:name=".MainApplication"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:allowBackup="false"
            android:theme="@style/AppTheme">

            <activity
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
                android:launchMode="singleTask"
                android:windowSoftInputMode="adjustResize"
                android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>

            <!-- Audio capture foreground service — foregroundServiceType REQUIRED for Android 14+ -->
            <service
                android:name=".AudioCaptureService"
                android:enabled="true"
                android:exported="false"
                android:foregroundServiceType="microphone" />

        </application>
    </manifest>
    ```

    CRITICAL: `android:foregroundServiceType="microphone"` on the service element is non-negotiable for Android 14. Without it, mic access from background is silently denied with no error.

    Also edit `NoScamAndroid/android/app/build.gradle` to ensure compileSdkVersion and targetSdkVersion are at least 34 (required for FOREGROUND_SERVICE_MICROPHONE):

    ```gradle
    android {
        compileSdkVersion 34
        ...
        defaultConfig {
            ...
            targetSdkVersion 34
            minSdkVersion 24
        }
    }
    ```

    Set minSdkVersion to 24 (Android 7.0) — broad enough for demo, SYSTEM_ALERT_WINDOW TYPE_APPLICATION_OVERLAY requires API 26+ but 24 is the RN 0.74 minimum.
  </action>
  <verify>
    Run: `grep -c "foregroundServiceType" NoScamAndroid/android/app/src/main/AndroidManifest.xml`
    Must output `1` (exactly one occurrence).

    Run: `grep "FOREGROUND_SERVICE_MICROPHONE" NoScamAndroid/android/app/src/main/AndroidManifest.xml`
    Must return the permission line.

    Run: `grep "SYSTEM_ALERT_WINDOW" NoScamAndroid/android/app/src/main/AndroidManifest.xml`
    Must return the permission line.

    Run: `grep "compileSdkVersion 34" NoScamAndroid/android/app/build.gradle`
    Must return a match.
  </verify>
  <done>
    AndroidManifest.xml contains all 5 permission declarations and the AudioCaptureService element with foregroundServiceType="microphone". build.gradle uses compileSdkVersion 34 and targetSdkVersion 34.
  </done>
</task>

</tasks>

<verification>
1. `ls NoScamAndroid/node_modules/react-native-audio-record` returns a directory listing (not "No such file").
2. `ls NoScamAndroid/node_modules/@supersami/rn-foreground-service` returns a directory listing.
3. `grep "foregroundServiceType=\"microphone\"" NoScamAndroid/android/app/src/main/AndroidManifest.xml` returns a match.
4. `grep "FOREGROUND_SERVICE_MICROPHONE" NoScamAndroid/android/app/src/main/AndroidManifest.xml` returns a match.
5. `grep "SYSTEM_ALERT_WINDOW" NoScamAndroid/android/app/src/main/AndroidManifest.xml` returns a match.
</verification>

<success_criteria>
- Bare React Native 0.74 TypeScript project exists at C:/Code/noscam/NoScamAndroid/
- All three native modules installed: react-native-audio-record, react-native-float-bubble, @supersami/rn-foreground-service
- AndroidManifest.xml has all 5 permissions (RECORD_AUDIO, SYSTEM_ALERT_WINDOW, FOREGROUND_SERVICE, FOREGROUND_SERVICE_MICROPHONE, REQUEST_IGNORE_BATTERY_OPTIMIZATIONS) and AudioCaptureService with foregroundServiceType="microphone"
- build.gradle targets compileSdkVersion 34 and targetSdkVersion 34
</success_criteria>

<output>
After completion, create `.planning/phases/03-android-client/03-01-SUMMARY.md`
</output>
