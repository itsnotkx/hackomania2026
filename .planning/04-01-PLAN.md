---
phase: 04-integration-and-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/renderer/overlay.html
  - electron/renderer/overlay.css
  - electron/renderer/overlay.js
  - electron/main/main.js
autonomous: true
requirements:
  - OVER-02
  - OVER-04

must_haves:
  truths:
    - "Clicking the Electron badge opens an expanded card showing confidence score (e.g. '87% AI-generated'), a one-line explanation, and 10 colored history dots"
    - "Moving the sensitivity slider takes effect on the next chunk — the badge state changes based on the new threshold, not the old one"
    - "Closing the card collapses it back to the badge with a CSS fade-out transition"
  artifacts:
    - path: "electron/renderer/overlay.html"
      provides: "Collapsed badge + expanded card DOM structure"
      contains: "id=\"card\""
    - path: "electron/renderer/overlay.css"
      provides: "Card show/hide transitions, badge color states, dot styles"
      contains: ".card.hidden"
    - path: "electron/renderer/overlay.js"
      provides: "Ring buffer, threshold logic, expand/collapse, IPC resize calls"
      contains: "getLabel"
    - path: "electron/main/main.js"
      provides: "IPC handler for window resize on card expand/collapse"
      contains: "ipcMain.on"
  key_links:
    - from: "electron/renderer/overlay.js"
      to: "electron/main/main.js"
      via: "ipcRenderer.send('resize-window') on expand/collapse"
      pattern: "ipcRenderer\\.send.*resize"
    - from: "electron/renderer/overlay.js"
      to: "badge state machine"
      via: "getLabel(result.score, threshold) on each WS message"
      pattern: "getLabel"
---

<objective>
Add the expanded detail card and sensitivity threshold slider to the Electron overlay.

Purpose: This delivers OVER-02 (expanded card with confidence, explanation, history dots) and OVER-04 (sensitivity slider) for the PC client, completing the full demo interaction flow on Windows — badge turns red, user clicks, sees 87% confidence with rolling history.

Output: Updated Electron renderer with clickable badge, animated card expansion, ring buffer history, score/explanation text, and sensitivity slider. BrowserWindow resizes via IPC on open/close.
</objective>

<execution_context>
@C:/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-and-polish/4-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Electron expanded card UI with ring buffer and score display</name>
  <files>
    electron/renderer/overlay.html
    electron/renderer/overlay.css
    electron/renderer/overlay.js
    electron/main/main.js
  </files>
  <action>
In overlay.html, add the expanded card DOM structure alongside the existing collapsed badge. The card must contain: a score line (`<div id="score-text">` showing "87% AI-generated"), an explanation line (`<div id="explanation-text">`), a history dot row (`<div id="history-dots">`), a close button (`<button id="close-card">`). The badge div gets an onclick handler to expand. Keep existing badge state elements intact.

In overlay.css, add the card styles with CSS transition for show/hide:
```
.card { transition: opacity 0.2s ease-in-out, transform 0.2s ease-in-out; }
.card.hidden { opacity: 0; transform: translateY(10px); pointer-events: none; }
```
Add badge color classes: `.badge-human { background: #22c55e; }`, `.badge-ai { background: #f59e0b; animation: pulse 1.2s infinite; }`, `.badge-analyzing { background: #6b7280; }`, `.badge-disconnected { background: #374151; }`. Add `@keyframes pulse` (scale 1.0 → 1.05 at 50%). Add dot styles: `.dot { width: 10px; height: 10px; border-radius: 50%; display: inline-block; }`.

In overlay.js, implement a ring buffer of max 10 results using `chunkHistory` array (`slice(-(MAX_HISTORY-1))`). Add `getExplanation(score)` function using score bands from the research file (>0.85 = "Strong spectral artifacts...", >0.65 = "Possible spectral artifacts...", >0.45 = "Ambiguous signal...", else "Natural voice characteristics..."). Add `renderHistoryDots(history)` that creates dot divs colored by label (AI = #f59e0b, HUMAN = #22c55e, empty slots = #d1d5db). Add `expandCard()` function that removes `.hidden` from card div and sends `ipcRenderer.send('resize-window', { height: 320 })`. Add `collapseCard()` that adds `.hidden` back and sends `ipcRenderer.send('resize-window', { height: 64 })`. Wire expand to badge onclick and collapse to close button.

Update the existing WebSocket `onmessage` handler to: (1) push result into chunkHistory ring buffer, (2) call `renderHistoryDots()`, (3) update `score-text` and `explanation-text`, (4) apply badge color class.

In electron/main/main.js, add `ipcMain.on('resize-window', (event, { height }) => { mainWindow.setSize(mainWindow.getSize()[0], height); })`. Ensure `ipcMain` is imported from electron.
  </action>
  <verify>
    Run `npm start` in the electron directory. Click the badge — card must expand with score text, explanation, and 10 dot placeholders (grey until results arrive). Click close — card must collapse. Run `npm start` in dev mode with devtools, confirm no console errors on expand/collapse.
  </verify>
  <done>
    Badge click expands card; card contains score line, explanation line, and dot row; close button collapses card; BrowserWindow height changes on expand/collapse; no JS errors in renderer console.
  </done>
</task>

<task type="auto">
  <name>Task 2: Electron sensitivity threshold slider wired to badge state</name>
  <files>
    electron/renderer/overlay.html
    electron/renderer/overlay.css
    electron/renderer/overlay.js
  </files>
  <action>
In overlay.html, add inside the expanded card (before the close button):
```html
<div class="slider-row">
  <label for="threshold-slider">Sensitivity</label>
  <input type="range" id="threshold-slider" min="0.5" max="0.95" step="0.05" value="0.7" />
  <span id="threshold-value">0.70</span>
</div>
```

In overlay.css, add `.slider-row { display: flex; align-items: center; gap: 8px; margin-top: 8px; }` and `#threshold-slider { flex: 1; }`.

In overlay.js:
1. Add `let threshold = 0.7;` as module-level state.
2. Wire `threshold-slider` input event: `document.getElementById('threshold-slider').addEventListener('input', (e) => { threshold = parseFloat(e.target.value); document.getElementById('threshold-value').textContent = threshold.toFixed(2); })`.
3. Implement `getLabel(score, threshold)` function: if `score > threshold` return `'AI'`; if `score < (1 - threshold)` return `'HUMAN'`; else return `'UNCERTAIN'`. An UNCERTAIN label maps to the ANALYZING badge color (#6b7280).
4. Update the WebSocket `onmessage` handler to call `getLabel(result.score, threshold)` on each incoming result (replacing any hardcoded label usage) before updating badge color class. The badge color class must change immediately on the next chunk using the current slider value.

Badge CSS transition already handles smooth color change (added in Task 1). No additional animation code needed.

Do NOT apply threshold retroactively to history dots — history dots use the backend's raw `result.label` value, not the threshold-adjusted display label. This is intentional per the research doc.
  </action>
  <verify>
    Start the Electron app with a running backend. Open the card, move slider from 0.7 to 0.5 — on the next chunk the badge should flag more AI results. Move to 0.9 — badge should flag fewer. Check that history dots remain unchanged (they show raw backend labels, not threshold-adjusted). Run `npm run lint` if a lint script exists.
  </verify>
  <done>
    Slider in expanded card shows current threshold value; moving slider updates threshold immediately; badge state on next chunk reflects new threshold; history dots are unaffected by slider changes; threshold display label updates in real time.
  </done>
</task>

</tasks>

<verification>
1. `npm start` in electron directory launches overlay with no errors.
2. Badge click expands card showing score, explanation text, and 10 dots (grey placeholders before results).
3. After several chunks, history dots update with correct colors (green/amber).
4. Sensitivity slider in card changes threshold value display and affects badge on next chunk.
5. Close button collapses card back to badge.
6. BrowserWindow resizes between collapsed (64px tall) and expanded (320px tall) states.
</verification>

<success_criteria>
- Clicking badge opens expanded card with confidence score, one-line explanation, and 10-slot history dot row
- Sensitivity slider changes threshold; badge state reflects new threshold on next received chunk
- History dots show last 10 chunk results colored by raw backend label (AI=amber, HUMAN=green, empty=light grey)
- Card expand/collapse animated via CSS transitions (0.2s ease-in-out)
- BrowserWindow height adjusts to content via IPC
- No regressions: collapsed badge still shows correct 4-state colors (HUMAN/AI/ANALYZING/DISCONNECTED)
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-and-polish/04-01-SUMMARY.md`
</output>
